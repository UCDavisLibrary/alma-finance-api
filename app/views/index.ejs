<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('include/head'); %>
  </head>
  <body>
    <%- include('include/header'); %>
    <div class="l-container pt-2">
      <h2>Payments App</h2>
      <% if (isUser) { %>
      <p>Hello, <%= userName %>! Here are your options:</p>
      <ul>
        <li><a href="/preview">Select invoices to send to Oracle.</a></li>
        <li>
          <a href="/oraclestatus">Check the status of sent invoices.</a>
        </li>
      </ul>
      <% } else { %>
      <p>You are not logged in. Please log in to access the forms.</p>
      <% } %>
    </div>
    <%- include('include/footer'); %>
  </body>
</html>
