{
  "name": "alma-sync",
  "version": "1.0.0",
  "description": "Standalone Alma reference-data sync worker for alma-finance",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/db.js && node --check src/alma-client.js && node --check src/repositories.js && node --check src/log.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mysql2": "^3.11.5"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
